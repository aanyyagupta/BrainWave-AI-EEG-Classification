"""
src package
------------
WHY THIS FILE EXISTS:
    This empty (well, documented) file tells Python "the src folder is a
    package," which means other files can do:
        from src.preprocessing import handle_missing_values
    instead of messy relative imports. It's a Python convention -
    every folder you want to import from needs an __init__.py file.
"""
